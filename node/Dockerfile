# Contenedor de build del monorepo CodeAlkimia.
# Node LTS + corepack habilitado (pnpm disponible en PATH para cualquier uid;
# la versión concreta de pnpm la fija el packageManager del package.json raíz).
FROM node:22-bookworm

RUN corepack enable
